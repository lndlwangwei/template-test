<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="{{base_package}}.{{package_path}}.support.{{domain}}Repository">

    <cache/>
    <sql id="cols">qid, courseid, audited, auditstatus, auditor, auditdate, updatetime</sql>

</mapper>